trigger: none

schedules:
- cron: '0 0 * * *'
  displayName: Daily midnight build
  always: true
  branches:
    include:
    - backend/main
    - backend/dev

jobs:
  - template: ./jobs/reset-site.template.yml
    parameters:
      jobName: reset_dev
      environment: dev

  # - template: ./jobs/reset-site.template.yml
  #   parameters:
  #     jobName: reset_live
  #     environment: live